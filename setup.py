
from setuptools import find_packages, setup

setup(
    name='ml_in_prod_hw01',
    version='0.0.0',
    description='ml_in_prod_hw01',
    packages=find_packages(exclude=['tests*', ]),
    package_data={},
)
